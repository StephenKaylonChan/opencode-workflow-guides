# Session Notes

## Current Focus

- 使用仓库自身的 OpenCode 配置长期维护 `guides-opencode`
- 让新开会话时可以直接 `/catchup` 后进入实际维护工作

## Completed

- 已建立主指南、模板、roadmap 和 research 体系
- 已为仓库自身创建 `opencode.jsonc`、`.opencode/`、`docs/architecture/`、`docs/development/`
- 已把当前维护重点写入 `instructions` 和 `catchup` 命令

## Next Steps

- 下一轮用 OpenCode 实际执行一次 `/catchup`、`/audit`、`/test`
- 记录真实使用过程中的摩擦点
- 形成“推荐最小安装集”，再回写到主文档和模板

## Risks / Notes

- 仓库已完成结构初始化，但还缺一次真实 OpenCode 操作回路验证
- 当前 `templates/base-project/` 仍偏“全量模板”，后续应再提炼最小安装方案
- 维护本仓库时，优先把主文档和模板视为一体，不要只更新其中一侧
